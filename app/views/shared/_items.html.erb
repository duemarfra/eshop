<%= link_to t(".add"), new_item_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.business? %>
<%= link_to t(".my_items"), user_path(Current.user.username), class:"mx-2 text-teal-500 font-bolt" if Current.user&.business? %>
<%= link_to t(".favorites"), favorites_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.customer? %>
<%= link_to t(".shoppings"), shoppings_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.customer? %>
<%= link_to t(".sales"), shoppings_sale_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.business? %>
<%= link_to t(".categories"), categories_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.admin? %>
<%= link_to t(".users_list"), admins_path, class:"mx-2 text-teal-500 font-bolt" if Current.user&.admin? %>
<%= link_to t(".login"), new_session_path, class:"mx-2 text-teal-500 font-bolt" unless Current.user %>
<%= link_to t(".register"), new_user_path, class:"mx-2 text-teal-500 font-bolt" unless Current.user %>
<%= link_to t(".logout"), session_path(Current.user.id), data: {
                                                            turbo_method: :delete,
                                                            turbo_confirm: t("common.confirm"),
                                                            }, class:"mx-2 text-teal-500 font-bolt" if Current.user %>